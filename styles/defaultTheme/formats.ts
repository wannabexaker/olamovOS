const formats = {
  dateModified: {
    hour: "numeric",
    hour12: true,
    minute: "2-digit",
  } as Intl.DateTimeFormatOptions,
  systemFont: "'Lucida Console', 'Courier New', monospace",
};

export default formats;
